# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.mypy_cache/

# ML / data (narrow ignores so pipeline artifacts/*.csv stay visible to DVC/git)
data/processed/
data/raw/*.csv
data/*.zip
data/south_german_extract/

# UCI South German Credit UPDATE (.asc) can be committed for CI + reproducibility (CC BY 4.0)

# MLflow & DVC
mlruns/
mlflow.db
.dvc/cache/

# Train outputs (rebuilt by `dvc repro`)
artifacts/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
